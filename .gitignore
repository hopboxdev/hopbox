/bin/
/dist/
*.db
*.db-shm
*.db-wal

# Go test crash dumps (anchored: must NOT match the internal/core/ tree)
/core
/mesad
/mesa-provider

# built control-plane binary (use /bin or go run)
/hopboxd
/boxd

# stray SSH CA from running a daemon in the repo dir
hopbox-ssh-ca
*.db
